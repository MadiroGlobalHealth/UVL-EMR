{
  "_comment": "Copy to sites.json (untracked) and fill in. A domain is resolved to the host that runs its stack, because the checks read the databases through `docker exec`.",

  "uvl-emr-uat.madiro.org": {
    "ssh": "ubuntu@UAT_HOST",
    "ssh_port": 2222,
    "project": "ozone-msf-mugamba"
  },
  "uvl-emr.madiro.org": {
    "ssh": "USER@PROD_HOST",
    "project": "ozone-msf-mugamba"
  }
}
